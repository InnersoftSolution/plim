# As variáveis ficam por app (cada um carrega o seu):
#   apps/web/.env.local  → front (VITE_AUTH_PROVIDER, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY)
#   apps/api/.env        → API   (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
# Veja os .env.example dentro de cada app.
