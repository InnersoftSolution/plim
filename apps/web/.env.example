# Provedor de autenticação: "demo" (UI sem backend) ou "supabase" (real).
VITE_AUTH_PROVIDER=demo

# Supabase (somente auth no front). Pegue em: Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
